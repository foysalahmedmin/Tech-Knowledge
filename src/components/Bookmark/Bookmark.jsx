import React from 'react';

const Bookmark = (props) => {
    console.log(props.totalTime);
    return (
        <div className='w-[410px] bg-gray-300 ' >
            
        </div>
    );
};

export default Bookmark;